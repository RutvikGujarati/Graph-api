{
  "name": "api",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ api",
    "create-local": "graph create --node http://localhost:8020/ api",
    "remove-local": "graph remove --node http://localhost:8020/ api",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 api",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
